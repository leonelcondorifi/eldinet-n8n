services:
  n8n:
    image: docker.n8n.io/n8nio/n8n
    container_name: n8n
    ports:
      - "5678:5678"
    env_file:
      - .env
    volumes:
      - n8n-data:/home/node/.n8n
    command: ["start", "--tunnel"]

volumes:
  n8n-data:
