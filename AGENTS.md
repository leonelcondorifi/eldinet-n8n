# AGENTS

- Install deps from `requirements.txt` (no lockfile present).
- FastAPI entrypoint is `app/main.py` (`app` instance).
- Run the API locally: `uvicorn app.main:app --reload`.
- Run tests: `pytest` (tests live in `tests/`).
